#!/bin/sh

case "$1" in
  start)
    echo "Running Harbor register-file check"
    /usr/bin/mmio-test register-file check
    ;;
  stop|restart|reload)
    ;;
  *)
    echo "Usage: $0 {start|stop|restart|reload}"
    exit 1
    ;;
esac

exit 0
