#!/bin/sh

case "$1" in
  start)
    echo "Running Harbor register-file dry-run"
    /usr/bin/mmio-test register-file --dry-run
    ;;
  stop|restart|reload)
    ;;
  *)
    echo "Usage: $0 {start|stop|restart|reload}"
    exit 1
    ;;
esac

exit 0
