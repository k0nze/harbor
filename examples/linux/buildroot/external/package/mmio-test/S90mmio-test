#!/bin/sh

case "$1" in
  start)
    echo "Running Harbor SystemC register check"
    /usr/bin/mmio-test systemc-register check
    ;;
  stop|restart|reload)
    ;;
  *)
    echo "Usage: $0 {start|stop|restart|reload}"
    exit 1
    ;;
esac

exit 0
